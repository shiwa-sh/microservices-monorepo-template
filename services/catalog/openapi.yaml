openapi: 3.1.0
info: { title: catalog, version: 0.1.0, description: Product catalog service. }
servers: [{ url: /api/catalog }]
tags: [{ name: products }]
paths:
  /products:
    get:
      operationId: listProducts
      description: List all products.
      tags: [products]
      responses:
        "200": { description: The product list, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Product" } } } } }
        default: { $ref: "#/components/responses/Error" }
    post:
      operationId: createProduct
      description: Create a product.
      tags: [products]
      requestBody:
        required: true
        description: The product to create.
        content: { application/json: { schema: { $ref: "#/components/schemas/ProductInput" } } }
      responses:
        "201": { description: Created, content: { application/json: { schema: { $ref: "#/components/schemas/Product" } } } }
        default: { $ref: "#/components/responses/Error" }
  /products/{id}:
    get:
      operationId: getProduct
      description: Fetch a product by id.
      tags: [products]
      parameters: [{ name: id, in: path, required: true, description: Product id., schema: { type: string, format: uuid } }]
      responses:
        "200": { description: The requested product, content: { application/json: { schema: { $ref: "#/components/schemas/Product" } } } }
        default: { $ref: "#/components/responses/Error" }
components:
  responses:
    Error:
      description: Error response
      content:
        application/problem+json:
          schema: { $ref: "#/components/schemas/Problem" }
  schemas:
    Problem:
      type: object
      description: RFC 7807 problem document.
      required: [code, message]
      properties:
        code:    { type: string, example: "not_found" }
        message: { type: string, example: "product not found" }
        details: { type: object, additionalProperties: true, example: { field: "name" } }
    Product:
      type: object
      description: A catalog product.
      required: [id, name, price_cents]
      properties:
        id:          { type: string, format: uuid, example: "3fa85f64-5717-4562-b3fc-2c963f66afa6" }
        name:        { type: string, example: "Aeron Chair" }
        price_cents: { type: integer, minimum: 0, example: 149900 }
    ProductInput:
      type: object
      description: Request body to create a product.
      required: [name, price_cents]
      properties:
        name:        { type: string, minLength: 1, example: "Aeron Chair" }
        price_cents: { type: integer, minimum: 0, example: 149900 }
