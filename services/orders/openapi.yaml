openapi: 3.1.0
info: { title: orders, version: 0.1.0, description: Order checkout and saga orchestration service. }
servers: [{ url: /api/orders }]
tags: [{ name: orders }]
paths:
  /orders:
    post:
      operationId: checkout
      tags: [orders]
      description: Starts the Checkout Temporal saga (ADR-0006).
      requestBody:
        required: true
        description: The checkout to start.
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CheckoutInput" }
      responses:
        "202":
          description: Accepted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/WorkflowHandle" }
        default: { $ref: "#/components/responses/Error" }
  /orders/{id}:
    get:
      operationId: getOrder
      description: Fetch an order by id.
      tags: [orders]
      parameters: [{ name: id, in: path, required: true, description: Order id., schema: { type: string, format: uuid } }]
      responses:
        "200": { description: OK, content: { application/json: { schema: { $ref: "#/components/schemas/Order" } } } }
        default: { $ref: "#/components/responses/Error" }
components:
  responses:
    Error:
      description: Error response
      content:
        application/problem+json:
          schema: { $ref: "#/components/schemas/Problem" }
  schemas:
    Problem:
      type: object
      description: RFC 7807 problem document.
      required: [code, message]
      properties:
        code:    { type: string, example: "not_found" }
        message: { type: string, example: "order not found" }
        details: { type: object, additionalProperties: true, example: { field: "product_id" } }
    WorkflowHandle:
      type: object
      description: Handle to an async Temporal workflow run.
      required: [id, run_id, status]
      properties:
        id:     { type: string, example: "checkout-7f3a" }
        run_id: { type: string, example: "9b1c2d3e-4f56-7890-abcd-ef0123456789" }
        status: { type: string, enum: [running, completed, failed, cancelled], example: running }
        result_url: { type: string, format: uri, description: "GET to fetch terminal status + result", example: "/api/orders/orders/7f3a" }
    CheckoutInput:
      type: object
      description: Request body to start a checkout.
      required: [product_id, quantity]
      properties:
        product_id: { type: string, format: uuid, example: "3fa85f64-5717-4562-b3fc-2c963f66afa6" }
        quantity:   { type: integer, minimum: 1, example: 2 }
    Order:
      type: object
      description: A customer order.
      required: [id, product_id, quantity, total_cents, status]
      properties:
        id:          { type: string, format: uuid, example: "3fa85f64-5717-4562-b3fc-2c963f66afa6" }
        product_id:  { type: string, format: uuid, example: "3fa85f64-5717-4562-b3fc-2c963f66afa6" }
        quantity:    { type: integer, example: 2 }
        total_cents: { type: integer, example: 3998 }
        status:      { type: string, enum: [pending, confirmed, failed], example: confirmed }
